# DGS_SVN — Legacy SVN Repository Mirror

Stability: C3 - Structural / stable

## Table of Contents

- [What It Is](#what-it-is)
- [Contents of `dgs/`](#contents-of-dgs)
- [`Documentation/` — Detector Database & Docs](#documentation--detector-database--docs)
- [`salvaged_notes/` — Key Historical Documents](#salvaged_notes--key-historical-documents)
- [`findFile.sh`](#findfilesh)
- [Usage / Status](#usage--status)
- [Cross-References](#cross-references)

---

## What It Is

A mirror/checkout of the DGS legacy **SVN (Subversion)** repository. This is the historical source of DGS code before the migration to Git.

Contents:
- `dgs/` — Main DGS SVN tree (large, many subdirectories)
- `findFile.sh` — Utility script to search within the SVN tree

**Local path:** `/home/dgsspark/DGS_tools_pack/DGS_SVN/dgs/`

---

## Contents of `dgs/`

The SVN tree contains a broad historical archive of DGS development:

| Directory | Contents |
|-----------|----------|
| `17pc030-GretinaTRGT` | **GRETINA Target Wheel Interface PCB** (2017, ANL part 17pc030) — Allegro `.brd` layout + Gerber PDF + `TargetWheelInterface.doc`. Interfaces the GRETINA target wheel mechanism to the DAQ/trigger system. Not DGS-specific; archived here as it was designed at ANL in the same era. |
| `17pc031-HeliosPreampPower` | **HELIOS Preamp Power Supply PCB** (2017, ANL part 17pc031) — Allegro `.brd` layout + OrCAD schematic (`.DSN`). Powers the HELIOS detector preamplifiers. Unrelated to DGS Gammasphere; archived alongside DGS PCBs as it was designed concurrently at ANL. |
| `20180921` | **Asyn driver source snapshot (Sep 21, 2018)** — 43 C/C++ files: `asynDigitizerDriver.cpp/.h`, `asynMTDriver_JTA.cpp/.h`, `asynRTDriver_JTA.cpp/.h`, `asynDebugDriver.cpp/.h`, `asynDigParams.c/.h`, `asynMTrigParams.c/.h`, `asynRTrigParams.c/.h`, `asynSDigParams.c/.h`, and others. Snapshot of the VME asyn driver codebase before Git migration; historical reference for the state of the driver in 2018. Current version lives in `DGS_tools_pack/vxworks/dgsDrivers/`. |
| `20230818_edm` | **EDM screen snapshot (Aug 18, 2023)** — 208 `.edl` files, more complete than `screens/` (141 files). Includes everything in `screens/` plus: CLO-variant screens (`clov*` — Clover detector?), legacy VXI screens (`VXI.edl`, `VXImain.edl`, `vxivolts.edl` — analog Gammasphere), `WheelMap.edl`/`WheelMapRBV.edl` (GS detector wheel geometry display), `StripeFPGAGeneric.edl` (generic StripeFPGA control), `ThrottleControl.edl`, `Trace.edl`, `TrigControl.edl`, `VMEDigMap.edl`, `xxx.edl`. Also includes two EDM launcher scripts: `dgscommander` (production DGS, CA 5064/5065, `/global/devel/` paths, launches `DGSrunControl.edl` read-only) and `dgscommander2` (test stand / SBX, CA 5074/5075, `/global/devel6/` paths, launches `DGSrunControl.edl` read-write). Both reference legacy Solaris/con6 paths — historical reference for the full EDM operator GUI in its final form before PyQt6 migration. |
| `con6_20220728` | con6 (Solaris host) snapshot *(not in local mirror)* |
| `con6_EPICS_base` | EPICS base from con6 era *(not in local mirror)* |
| `con6_work` | con6 working files *(not in local mirror)* |
| `daq_system_tags` | **DAQ system version snapshots** — full-system tagged releases, each containing firmware binaries and/or IOC + EDM config trees. Tags: `DFMA_20220711`, `DFMA_20220712` (July 2022 DFMA experiment: DIG=6194, MTRG=5542, RTRG=5814 or 4747); `DFMA_20220729`, `DFMA_20221021` (fuller tags with `firmware/`, `ioc/`, `edm/`, `softIOC/` dirs: DIG=6194, MTRG=5542, RTRG=4747, Digitizer_VME=3963, Trigger_VME=4485); `DUB_20211101` (DUBALL experiment: DIG=6194, MTRG=5542, RTRG=4747); `DXA_20220720` (X-Array experiment: same firmware); `SL6_DGS_20220923` (SL6 = DFMA+DXA combined? — contains all 5 firmware types with `XXXX` placeholder names, suggesting it was a template snapshot). `ioc/db/` has **both** old `dgs`-prefixed templates (`dgsMTrigUser.template`, `dgsMTrigRegisters.template`, `dgsRTrigUser.template`, `dgsRTrigRegisters.template`) AND the new unprefixed names (`MDigRegisters.template`, `MDigUser.template`, `SDigRegisters.template`, `SDigUser.template`) — the transition point between naming conventions. Also has two legacy templates not in current `ioc/`: `gretVME.template` (uses `DTYP="Gretina VME Board"` — old non-asyn VME driver, generated by VisualDCT v2.4; predates the asyn migration) and `onMon.template` (online monitor config: `DammSender` IP address + related `OnMon$(DN)` PVs for online sorting; dropped when online monitoring was moved out of the IOC). **IOC template naming evolution:** `DFMA_20221021/ioc/db/` uses `CS_Digitizer.template` + `CS_Digitizer_reg.template` with `$(P)$(R)` macro style and per-crate `VMExx_4mdig.db` files (17K+ lines combined) — pre-Git, pre-`MDigRegisters` naming era. By Sep 2022 (`SL6_DGS_20220923`) the `MDigRegisters`/`MDigUser`/`SDigRegisters`/`SDigUser` split existed alongside the old names. By Feb 2024 (`DB_backup_20240205`) only the new names remained, matching current `ioc/db/`. The `CS_` prefix, `4mdig` naming, `gretVME`, and `onMon` templates were all dropped during the Git migration. **`softIOC/db/JustGlobals.db`** (5,195 lines): a soft IOC that broadcasts global controls (e.g., `master_fifo_reset`, `trigger_mux_select`) to all VME crates via chained `dfanout` records (`GLBL:DIG:F01`→`F02`→...→per-crate `VMExx:GLBL:*`). Fan-out targets DFMA_20221021: VME01–VME08 + VME10 (9 crates — VME09, VME11, VME12 absent during DFMA experiment). ✅ verified 2026-04-09 — `DFMA_20221021/softIOC/db/JustGlobals.db:L1-30` + `ioc/db/` directory listing. Useful for: recovering exact firmware sets used in past experiments, cross-referencing with run data, tracing IOC naming evolution. |
| `Data_Generator` | **Trigger-chain test injector** — FPGA board that injects programmable discriminator bit patterns into the trigger chain (simulating digitizer hits) via SERDES links. Two firmware variants: `DSSD_MAIN_FPGA/` and `GRETINA_MAIN_FPGA/` (same VHDL, different target system). Contains a CPLD and main FPGA. Key features: 8 SERDES output channels, each with up to 1K discriminator bit patterns loaded via VME RAM tables (16-bit: bits[15:6]=pattern, bits[5:0]=assertion time in clocks); 9th table drives 2 NIM outputs synchronously. Patterns triggered by NIM pulse or Frame12 command (bit15=start now, bit14=start at timestamp rollover). Throttle output can be time-controlled via `MISC_CTL2_REG[0]` (always on) or timed to timestamp match via `THROTTLE_TIME_ON`/`THROTTLE_TIME_OFF` registers. CPLD drives throttle request lines (via connector A/B/C/D outputs at VME addrs E000/E004/E008/E00C; SUM output at E010; throttle bit pattern at E014); main FPGA drives discriminator bits — reversed relative to normal Router pinout because the cable swaps SERDES_IN↔OUT and discriminator↔throttle pins. `MISC_CTL` bit controls one-shot vs wrap-around of read pointer. See `DataGenOperationNotes.txt`. |
| `DB_backup_20240205` | **EPICS DB snapshot (Feb 5, 2024)** — templates and per-crate `.db` files as they existed before the Git migration. Templates: `asynDebug.template`, `daqCrate.template`, `daqSegment2.template`, `MDigRegisters.template`, `MDigUser.template`, `MTrigRegisters.template`, `MTrigUser.template` (+ `.TMP` draft), `RTrigRegisters.template`, `RTrigUser.template`, `SDigRegisters.template`, `SDigUser.template`, `RunProtect.asf`. Per-crate `.db` files: `dgsGlobals_DGS_VME01.db` through `VME12.db` + `VME99.db`. **Notable difference vs current `ioc/db/`:** VME-variant templates (`MDigRegistersVME`, `MDigUserVME`, `SDigRegistersVME`, `SDigUserVME`) did NOT exist in Feb 2024 — added later for crates 66 and 99 to expose VME FPGA status PVs (power/voltage/temp sensors, clock select, `vme_code_revision_RBV`, `SERIAL_NUMBER_RBV`). `asynDebug.template` removed from ioc/db/ in Git version. **`MTrigRegisters.TMP.template`** is an intermediate draft that predates the final `MTrigRegisters.template` — missing ~30 PVs that were added before release: `reg_DiagnosticA_RBV`–`reg_DiagnosticH_RBV` (per-link SERDES diagnostics), `reg_MON7_FIFO_DEPTH_RBV`, `reg_MON_FIFO_STATE_RBV`, `reg_CHAN_FIFO_STATE_RBV`, `reg_LINK_LRU_MACH_STAT_RBV`, `reg_NIM1_DELAY_RBV`, `reg_NIM2_DELAY_RBV`, `reg_LOCAL_TRIG_DELAY_RBV`, `reg_REMOTE_TRIG_DELAY_REG_RBV`, `reg_REMOTE_TRIG_DIG_OFFSET_RBV`, `reg_REMOTE_TRIGGER_TS_OFFSET_RBV`, `reg_SYNC_BUS_RBV`. These readback PVs represent monitoring/diagnostic additions made in the final push before Git migration. |
| `Detector_Repair` | **HPGe detector repair documentation**: `DetectorRepairProcedure.docx` (procedure guide), `Gammasphere Basics.docx` (GS detector basics), `AOEffectofannealingJINST.pdf` (anneal effect on detector performance, JINST paper), `document.pdf` (LANL report LA-UR, "High Purity Germanium Detector Repair" by Aaron Kilburn, 52nd INMM Annual Meeting — covers HPGe repair techniques from a national security/non-proliferation perspective), `Photos/` (tutorial + follow-up session photos), `Data Sample/` (MCA spectrum data files in legacy format, e.g. `data_sample.txt` with MCA channel/energy calibration header). Historical reference for crystal repair and annealing procedures. |
| `devel8`, `dgs_devel8` | Development branch 8 *(not in local mirror)* |
| `devel_tracker` | Development tracking *(not in local mirror)* |
| `DGS1_clean_folders` | DGS1 (old system) clean directory tree *(not in local mirror)* |
| `DGS1_total_backup` | Full DGS1 backup *(not in local mirror)* |
| `dgsext` | DGS extensions *(not in local mirror)* |
| `DGSFiberExpander` | **VME Fiber Adapter / Fiber Expander** — PCB #3174 (ANL part `21pc032`, Rev A, Sept 2021). Adapts VME backplane fiber connections to front-panel SFP/fiber connectors. Contains OrCAD schematics, Allegro layout, Gerber fab package, BOM, DRC, and front panel drawings. Vendor quote from Q74173A1 / QT-2873. Likely used to extend SERDES fiber links between crates or VME chassis. |
| `dgsSoftIOC` | Soft IOC (pre-Git version) *(not in local mirror)* |
| `ArdisiaDocuments.zip` | Ardisia documentation |
| `DigitizerFanout` | **Digitizer Fanout PCB** (Aug 2022): RJ45-based LVDS signal fanout board. BOM includes dual LVDS receivers (SN65LVDT9637B ×3), LVDS drivers (FIN1001M5X ×2), clock buffers (NB6N11SMNG ×2), OR/NOR logic gates, RJ45 jacks (Amphenol RJHSE5387 ×2 + vertical male plugs ×4), RJ11 connector ×1. Purpose: fan out SERDES/LVDS differential signals across RJ45 links — likely used to split digitizer trigger/data links to multiple destinations. OrCAD schematic + Allegro layout + BOM + DRC included. |
| `Digitizer_Tester` | **Digitizer Tester module** — generates arbitrary analog waveforms to exercise DGS/GRETINA digitizer boards without real detector signals. FPGA: Xilinx Virtex-4 `XC4VLX40-10FFG1148C`. Two sub-FPGAs: `MAIN_FPGA/` (waveform generation + SERDES/TTCL interface) and `VME_FPGA/` (VME A32/D32 access; bitfiles `DigTesterVME_20090928.mcs`, `20110516-VME.mcs`, `DigTesterVME_20120509.mcs`). Also `A32D32_VME_FPGA/` (A32/D32 VME bridge variant). `MAIN_FPGA/Source/` has 18 VHDL files. Key register map (from `Registers.vhd`): reg(4–5)=`reg_pulsed` (32b pulse config), reg(6)=`tester_config`, reg(7)=`aux_config`, reg(8)=`rj45_led_config`, reg(9)=`rj45_out_config`, reg(10–11)=`dig_width` (32b), reg(12–21)=`chan_config[0–9]` (8b each, per-output-channel source select), reg(22–24)=DAC SPI/data/power config, reg(25)=`dac_mode`, reg(26–27)=`dac_aux0/1`, reg(28–29)=`dac_gain0/1`, reg(30)=`dac0_constant`, reg(31)=`dac1_constant`, reg(32–34)=`dac0_prescale[0–2]`, reg(35–37)=`dac1_prescale[0–2]`, reg(39)=DAC0/DAC1 DIN mux select (12-bit: 6 fields × 2b each), reg(40–41)=`ramp0/1_prescale`, reg(42)=`serdes_config`, reg(43)=`serdes_tx_data`, reg(48)=`dac_config_trig`, reg(49–52)=`dac0/1_ts_mask` (32b timestamp match masks), reg(53–54)=`nim_delay[0–1]`, reg(55–56)=`dac0/1_burst_num` (16b), reg(57–60)=`dac0/1_burst_per` (32b), reg(61–62)=`nim_stretch[0–1]`, reg(63)=`nim_config`. Waveform BRAMs for DAC0 and DAC1 addressed at register block offsets 0x200–0x3FF (by BRAM addr select bits[15:14]). See `knowledgeBase/digitizer_tester.md` for full details. |
| `Documentation` | **General DGS documentation archive**: `North_db.csv` + `South_db.csv` — 2017 physical detector installation database (110 rows, 55 north + 55 south); columns: GS hole#, Hoze#, Ge Det#, Ge HV (V), SB#, SB position, BGO#, BGO install date. 82/110 holes have HV data: range 3000-4800V, most common 4000V (19 holes), 3500V (16 holes). `Formal/Software/` — software docs including: `HowCarlware-TimwareWorks.docx` (T. Madden — **earliest DGS EPICS architecture doc**: describes original Carlware/Timware template/PV naming (`dgsDigRegisters.template`, `dgsDigUser.template`, `daqSegment.template`, `gretVME.template`, `link.template`, `asynDebug.template`, `dgsGlobals_DGS_GLBL.db`); trigger PV naming was `Trig0_XXX` (MTRG) / `Trig1_XXX` (RTR1) — predates current `VMExx:MDIG1:` convention. Shows crate range VME00–VME11 + VME32 (trigger). `GammaWare.docx` (3 MB — analysis software framework documentation, ~2013). `howTheSenderWorks.docx` (T. Madden — describes **original pre-`SendReceiveSupport.c` data path**: Dig FIFO → DMA via `sysVmeDmaInit()` + DMA mutex → `gDigRawQ`/`gDigRawRetQ` message queues; **400 buffers × 512 KB** = 200 MB total shared across all 4 DIGs per crate; `cesort.c`/`BuildSend.st` for event sorting; `OnMonitorSend.c` for UDP monitoring; `TrigCon.st` for throttle. Confirms architecture later replaced by `SendReceiveSupport.c` + current 200 × 1 MB buffer scheme in 2023); `DigitalGammasphereSW.pdf`, `asynDebugDriver*.pdf`, `IOC code design notes 20210914.pptx`. `Formal/Unsorted Docs/` — historical docs: 1994/1995 BGO timing + DAQ overviews, CDR (Oct 2005), DCBAL.doc, cable specs, GS papers; **`pv list digitizer.txt`** (1,620-line VisualDCT-format EPICS DB, prefix `Dig$(DB)_` — earliest known DGS1/Gretina PV naming before migration to `MOD###_MDIG1_` convention; register addresses in hex e.g. 0x0, 0x8, 0x10; record types: ai/ao, bi/bo, mbbi/mbbo, waveform; CAMAC-style `#C$(DC) S0xNN @` INP/OUT links). `MeetingNotes/` — meeting notes 2010-2011. |
| `edmhelp`, `edm-master`, `20230818_edm` | Legacy EDM screen utilities and dated snapshots |
| `screens/` | **Legacy EDM control screens** (141 `.edl` files): full DGS operator GUI before PyQt migration — includes `BigSummary.edl`, `CAGMrunControl.edl`, `BGSrunControl.edl`, IOC status, BGO rates, CLO (collector) channel/board/global screens, trigger lock, digitizer enable, analog GS, aux I/O control, acq status. Historical reference; current GUI is PyQt6 (`ANLDAQ/`). |
| `sbxscreens/` | **SBX EDM screens + commissioning scripts**: 20 `.edl` files (CollectorBox, PowerBoard, PickoffCtl, PickoffStatus, PreampData, SlopeBox_CTL, SlopeBoxrunControl, SlopeBoxSelect, RAM_Buffer, dgsTrigCntrl, etc.) + `PV.list` (201-line doc of CAMAC I/O record types: bi/bo, mbbi/mbbo, ai/ao with ANDmask, shift-factor, read-modify-write semantics) + `Std_Test.sh` (SBX cross-test setup script by JTA 2021-04-02: sets ScanMode=3, Ge/BGO HV off, DAC offsets=150, Ge threshold=600, then BGO HV0-13 all to 180, enables HV, sets `PARST_AutoClampDwell=10000`) + `D0_Std_Test.sh` / `D99_Std_Test.sh` (same for detector 0 and 99). Nominal BGO HV: **180 DAC units** per tube (from Std_Test.sh). |
| `FromT` | **Hardware files from Todd (trigger system history)**: contains PCB design packages, bitfiles, schematics, and legacy trigger docs. Key items:<br>• `06pc057d/` — PCB #06pc057 (likely early MTRG/RTRG carrier or adapter board); Gerber, BOM, placement files<br>• `12pc006a FLIC-FTK/` — FLIC-FTK (Fiber Link Interface Card — Front-end Trigger module, 2012); Gerbers, front panel DWG, BOM<br>• `12pc007b-FLIC-RTM/` — FLIC-RTM (Rear Transition Module for FLIC); Gerbers, front panel DWG<br>• `13pc003a Myriad/` — MγRIAD PCB (2013); Gerbers + Allegro board files (also see `knowledgeBase/myriad.md`)<br>• `17pc001a-ProtoDUNE-SSP/` — ProtoDUNE SSP (Signal Shaping Processor) hardware from 2017; not DGS-specific<br>• `Scarlet Trigger/` — **Pre-DGS multi-crate CAMAC trigger system** (2005) by John Weizeorick: PC + CAMAC-based busy/trigger fanout over RS485/RJ45; master→slave architecture; FPGA bitfiles (`m1trg.bit`, `strg.bit`) for Mesa 5i20 I/O card + accompanying VHD source; manual included. Historical predecessor to the DGS MTRG/RTRG FPGA trigger.<br>• `sspCABLE/` — SSP cable pinout CSVs<br>• `PrintCapture` software + `AUTORUN.INF` — Windows oscilloscope print-capture utility (not DGS-specific)<br>• Various `.DAT`/`.IVX`/`.LBL` files — likely OrCAD/Cadence netlist exports for other PCB designs |
| `GRETNA_CPLD_CHECK` | **GRETINA-era fast-strobe CPLD source** (2008, J. Anderson) — VHDL for both Master and Router CPLD variants. `Master_CPLD/fast_strb.vhd`: sums 4-bit partial-multiplicity values from connectors A–D, compares to threshold (reg 0xE000), asserts FAST_STROBE to Main FPGA; Mask reg (0xA000) forces unused inputs to zero; individual per-connector counts readable at 0xA010–0xA01C. `Router_CPLD/fast_strb.vhd`: sums 8 discriminator bits (one per digitizer channel) into a 4-bit partial multiplicity, reports on SUM output to Master; local fast-strobe threshold per Router; unused inputs default HIGH — must be masked. Both include testbenches (`fs_top_tb.vhd`) and pin constraints (`pinlock.ucf`). Historical predecessor to DGS CPLD (see `deep_fpga_MTRG_CPLD.md`); functional design carried forward into DGS. |
| `gtReceiver` | **Legacy C++ data receiver + analysis template** (pre-ANLDAQ), v6.57 by M. Oberling. Connects to IOC TCP server, receives raw binary data, writes GEB-format files. Key SOE markers: `DIG_SOE=0xAAAAAAAA` (digitizer), `TRIG_SOE=0xAAAA0000` (trigger, upper 16 bits only). GEB types written: 14=`GEB_TYPE_DGS`, 15=`GEB_TYPE_DGSTRIG`. Build flags (compile-time): `WRITEGTFORMAT` (GEB header output, always on), `FILE_PER_CHANNEL` (one file per board×channel — active default), `FOLDER_PER_RUN` (subdirectory per run, active), `DUMP_UNKNOWN_DATA_TO_DISK` + `DEBUG_OUTPUT_FILE` (trigger/unknown data to `_DIAG` file, active). Limits: `MAXBOARDID=4095`, `MAXCHID=16`, `DIG_MIN_HEADER_LENGTH=3 words`, `TRIG_MIN_HEADER=8 words`, `REFORMATTED_HEADER=10 words`. Summary printed every 5s (`SUMMARY_OUTPUT_INTERVAL`). Poll interval 10µs (`MAXNS=10000`). Utility tools: `rcvr_merge` — k-way timestamp-ordered GEB merge (reads all matching files in CWD by prefix, outputs one merged GEB file; `argv[1]`=output, `argv[2]`=filename prefix; up to 1024 files, 512KB I/O buffer); `rcvr_unmerge` — splits merged GEB file back into per-type files; `rcvr_data_scrubber` — scans GEB file for corrupt/malformed events (checks SOE word, header length, waveform sanity; `TOTAL_HEADER_LENGTH_WORDS=13`). Full GEB type table (types 1–23) in `dgs_analysis.md`. `analysis_program_template.c` (514 lines, M. Oberling 2022-08-30): canonical C reference for reading GEB+DIG files — full 13-word DIG header bitfield extraction for both LED (type 7) and CFD (type 8), including the LED/CFD split fields in words 3–7 (e.g. `TRIG_MON_DET_DATA` in word 7[31:16] LED mode, `CFD_SAMPLE[0-2]` scattered across words 5–7 CFD mode, `TRIG_MON_XTRA_DATA` in word 5[15:0] LED). Word 13 flag bits: `flag_st`=bit10 (2nd threshold), `flag_pt`=bit11 (PARST match), `flag_pc`=bit12 (prev CFD valid), `flag_cf`=bit13 (coarse fired). Aligns with `data_structures.md` field names. Cross-platform (Linux + Windows/CodeBlocks). Superseded by `ANLDAQ/tcpReceiverMT` and `fastEventConstructor`. |
| `HELIOS_Preamp_data` | **HELIOS preamplifier characterization data (2016-03-30)**: `alpha_5K-0_2016-03-30_15-46-13.dat` (15 MB tab-delimited ASCII) — decoded DIG event packet data from an alpha-source run on a HELIOS preamplifier at 5K gain, with columns matching the full DIG event header (FIXED_AAAAAAAA, CH_ID, USER_DEF, PACKET_LENGTH, GEO_ADDR, EVENT_TIMESTAMP, HEADER_TYPE, EVENT_TYPE, HEADER_LENGTH, all flag bits, LAST_DISC_TIMESTAMP, SAMPLED_BASELINE, CFD samples, PRE_RISE_ENERGY, POST_RISE_ENERGY, PEAK_TIMESTAMP, PRE/POST_RISE samples). Also: `20160330_preamp.doc` (lab notes, Word format) + `alpha5k.xlsm` (22 MB Excel workbook for analysis/visualization). Historical data only — HELIOS is a separate ANL experiment that used DGS digitizers for preamp testing; not part of current Gammasphere operations. |
| `LBL_Digitizer` | Lawrence Berkeley National Lab digitizer firmware images — binary bitfiles (`chip_top_2.00_009D.bin`, `chip_top_2.00_006e.bin`) and VME MCS file (`vme-30-41.mcs`) from Dec 2014. Historical reference; ANL developed its own digitizer firmware independently. |
| `Miscellany` | Miscellaneous files — contains `MSRDPCLI.EXE` / `MSRDPCLI(1).EXE` (Windows RDP client), `tightvnc-1.3.10-setup.exe` (TightVNC installer). Likely dropped here for remote-access convenience; no DGS-specific content. |
| `MyRIAD` | **MγRIAD module files**: `Documentation/` — full PDF documentation set: `MyRIAD Abridged User Notes.pdf`, `MYRIAD_Module_Specification.pdf`, `MyRIAD User Manual.pdf`, photo (`myriad.jpg`); `MyRIAD_memory_map.xls` — register memory map spreadsheet; `argonne22013.xls` — likely calibration/characterization data from 2013; `Schematics/` — OrCAD schematic package (`.DSN`/`.OLB`) + netlist snapshots (`_20130108`, `_20150106`, `_20150107`). See `knowledgeBase/myriad.md` for documented facts extracted from these PDFs. |
| `NewBlackBox` | **Pre-EPICS collector box Pi code** — `RaspberryPi/PreEpicsCode/`: direct SPI communication to CtrlFPGA + StripeFPGA via bcm2835 library (no EPICS layer). Key files: `Scan_DVI_Power.c` (694 lines) + `extract_Eprom_b.c`. Hard-coded hardware calibration constants: ADC span 5V / 16-bit = **13,107 counts/V**; 3.3V≈43,353 cts, 2.5V≈32,767 cts, 1.8V≈23,593 cts, 1.2V≈15,728 cts. 48V current sensor TMCS1101A4B: 400mV/A → **5,243 counts/A**; danger threshold >2,700–3,000 cts (~0.5–0.6A), shutdown >5,000 cts. Ground fault: AD8236 across 22Ω, gain=100 → 28,835 counts/mA; fitted curve: ADCval≈5e6×Rdet^−0.731. ADC scanner: CtrlFPGA DPRAM bank 1 (addrs 128–255); fast scan DRATE='11' (23,739 sps) → 3.29–5.31ms update per 6 ADCs; slow scan DRATE='00' (1,831 sps) → 42.6–69ms update. GPIO DEVSEL bus: GPIO13(pin33)=DEVSEL0, GPIO23(pin16)=DEVSEL1, GPIO24(pin18)=DEVSEL2, GPIO25(pin22)=DEVSEL3. SPI0=bcm2835 native; SPI1 requires `dtoverlay=spi1-1cs` in `/boot/config.txt`. Predecessor to current `collectorboxpi/` EPICS soft IOC. |
| `NS_scripts` | BGO tuning + PV extraction scripts (see `knowledgeBase/utility_scripts.md`): `BGO_tune_v2.py`, `extract_PV.py`, `GS_nums.py`, PV list txt files |
| `OrCAD` | **OrCAD component library**: `MBO_LIB.OLB` — Cadence OrCAD compound document library file (part symbols/schematic components). Likely used across multiple PCB designs in this SVN archive. |
| `Schematics` | **PCB schematics directory**: two designs — `PaddleCard/` (SFP fiber paddle card: OrCAD `TRIG_PADDLE_SFP.DSN`/`.opj` + Allegro layout; likely the SFP fiber interface board between trigger crates) and `Trigger_IO_Adapter/` (trigger I/O adapter: `TRIGGER_IO_ADPATER.DSN`/`.opj`, `AUX_IO_Pin_Mapping.xlsx` spreadsheet documenting aux I/O pin assignments, OrCAD library + parts BOM). Key parts in Trigger_IO_Adapter: `MC10ELT24` (ECL→TTL translator), `MC10H125` (TTL→ECL driver), `PCF8574A` (I2C 8-bit I/O port expander) — NIM/ECL level conversion board, likely interfaces to MγRIAD or MTRG aux I/O. |
| `SchematicTagsForTodd` | Empty directory — was likely used to tag schematic versions for Todd (trigger hardware lead); no files present in this SVN snapshot. |
| `Patch_Panel` | **Signal patch panel PCBs**: OrCAD schematics + Allegro layout for two boards — PCB `11pc015` (GS input patch, Rev A) and PCB `11pc016` (front+rear panel, Rev A); `PropagationSheet.xlsx` (signal routing/propagation reference); `Pictures/` (board photos). These are the analog signal patch panels routing detector signals from the Gammasphere array to the digitizer inputs. |
| `psg` | **PSG — Code-Generating Spreadsheet system**: reads CSV register map files and auto-generates EPICS `.db` records + CSS/EDM screens. Contains `CodeGeneratingSpreadsheetGeneric/Projects/DGS_Rtrig/fromdgs1.txt` (8,392-line generated EPICS DB output for the Router Trigger — **889 records**: 353 bi, 286 bo, 114 longin, 83 longout, 27 mbbo, 26 mbbi; all with `DTYP=asynUInt32Digital`). For comparison, current `ioc/db/RTrigRegisters.template` + `RTrigUser.template` = 183 + 897 = **1,080 records** — showing the DB has grown since the PSG era. Config in `salvaged_notes/dgs_config.txt` references CSV sources: `DGSMasterTriggerRegisterMap.csv`, `DGSRouterTriggerRegisterMap.csv`, `MasterDigitizerRegisterMap.csv`. The CSV files and generator Python script itself are not present in this SVN snapshot. Historical precursor to the `Auto-generate EPICS DB` task (see QUEUE.md). Also contains `N126_target_wheel/` — **N=126 target wheel encoder interface** (auxiliary nuclear physics device for Gammasphere): Spartan-6 XC6SLX9-2TQG144 FPGA, Raspberry Pi EPICS IOC (`N126App_RevA/`), DRV8824 stepper + L6203 DC motor + quadrature encoder + 8-ch LTC1660 DAC + I²C power board/LED display. Uses same `PickoffLocalSerial` SPI framework as Collector Box and Slope Box Pi IOCs. Not part of main DGS trigger chain. See [`n126_target_wheel.md`](n126_target_wheel.md). |
| `salvaged_notes` | Recovered notes from old systems |
| `sbxl_osc` | **Early Pi-based SBX/pickoff IOC dev notes**: `diagdump.txt` (EPICS boot trace for `PickoffApp_RevC` on Pi, CA ports 5074/5075 for test stand), `EPICS boot trace 20201028.txt` + `EPICS_init_trace_20200911` (IOC startup logs), `Breakdown of globals.ods` (spreadsheet of global variables), `NonEpics_SPI/` (direct SPI access without EPICS), bcm2835-1.63 library, `NotesOnVPNandSVN.txt` (VPN/SVN setup for Pi). Predecessor to current `collectorboxpi/` repo. |
| `sbxscreens` | SBX EDM screens + commissioning scripts (see main table above) |
| `SlopeBoxExtension` | SBX hardware + firmware + GS_ID dongle (→ `sbx.md`) |
| `SlopeBoxInterface` | Slope box interface hardware: schematics, PCB layout, pickoff card design, power budget lab notes, RPi interface. `Documentation/LabNotes/SlopeBoxCurrentDraw.txt` (2018-05-07, JTA) measured per-rail current draw (no detector connected): +12V=105mA (1.26W), -12V=113mA (1.36W), +24V=21mA (0.5W), +5V=81mA (0.4W) → total ~3.5W idle, ~5.7W with HV on. Estimated total per-detector (slope box + Pickoff Rev B + RPi): ~22W → requires 802.3at PoE (30W/port); 802.3af (15.4W/port) insufficient. Power supply recommendation: CUI PYB30-Q48-T512 converter. ✅ verified 2026-04-15 — `SlopeBoxCurrentDraw.txt:L88` ("SUBTOTAL : call it 22W." = slope box 5.7W + Pickoff Rev B 6W + RPi 10W; +20% DC-DC overhead = 26.4W to PoE switch) |
| `slopebox_scripts` | BGO HV sweep + counter averaging scripts: `BGO_Sweep_test` (sweeps `GS000_BGO_HV0..13` from 0→250 DAC in steps, averages `GS000_BGO[1-7]Sum_counter`), `caget_avg` (shell: reads PV N times, averages numeric value), `Avg_all_BGO_count` (reads all 8 BGO counter PVs via `caget_avg`). Run from `/global/devel/scripts/`. Legacy — see `utility_scripts.md` for current Python equivalents. |
| `TrigToTrig` | **Trigger-to-trigger interconnect PCB** (PCB#1446 / Layout#06PC057, Rev A, June 6, 2021): passive LVDS signal interconnect board for GRETINA trigger module SERDES I/O. Contains **two CompactPCI Type B HM connectors** (P1, P2: `HM_TYPEB_RA_MALE_0` — 125-pin, 5×25 CPCI Type B), no active components. Purpose: re-maps SERDES LINK signals between two trigger module backplane connectors — functions as a wiring adapter/crossover between two GRETINA trigger crate slots. Net inventory (82 nets): **44 LINK signals** — 11 groups (A,B,C,D,E,F,G,H,L,R,U) × 4 signals each (e.g. `LINK_A0`–`LINK_A3`), matching GRETINA trigger SERDES link naming convention; **16 AB_LVDS signals** (`AB_LVDS0`–`AB_LVDS15`); **16 DE_LVDS signals** (`DE_LVDS0`–`DE_LVDS15`); **4 ULVDS signals** (`ULVDS0`–`ULVDS3`); **1 GND**. All signal nets have `ELECTRICAL_CONSTRAINT_SET='100 ohm stripline'` with `DIFFERENTIAL_PAIR` assignments — proper LVDS impedance controlled routing. OrCAD schematics + BOM + DRC report; Allegro layout; fab package `21KB001`. ✅ verified 2026-04-13 — `TrigToTrig/allegro/pstxnet.dat` (net list) + `TRIG_TO_TRIG.BOM` + `TRIG_TO_TRIG.DRC` |
| `VXI_database` | **Legacy analog Gammasphere EPICS database** (6 files, ~192K lines total; resm1–4: 2,233 records each ✅ verified 2026-04-08; resm5–6: 1,718 records each ✅ verified 2026-04-08): `resm1.db`–`resm6.db` — pre-DGS VXI-based DAQ EPICS records for the original analog Gammasphere system. PV prefix conventions (✅ verified 2026-04-11 — `resm1.db` grep): `MOD###_` (module/digitizer), `BGO###_` (BGO detector), `VXI###_` (VXI crate channel), `GE###_` (Ge detector, e.g. `GE###_DV_TRAP`), `CR<crate>_<slot>_` (per-crate-slot ADC/DAC), `HEARTBEAT` (watchdog). Record types: mbbo/mbbi, ai/ao, bi/bo, sub, waveform. Key PV groups: `BGO###_CS_PAT[1-7]` (BGO coincidence pattern bits), `BGO###_CS_DB[1-7]` (delay buffer settings), `BGO###_CS_100KV` (HV control), `BGO###_DS/DV_LRE/TDC` (leading-edge/TDC status/value), `VXI###_DV_GE_CFD/PUD/SIDE` (Ge CFD, pole-up/down, side settings), `MOD###_DV_EN` (module enable), `MOD###_HV_CON` (HV control sub record). Historical reference only — predates DGS digitizers. |
| `signoise.run/` | **Signal integrity simulation run directory** — SIS/Cadence SigXplorer project stub (`cases.cfg` + empty `case1/`). Likely a placeholder or leftover from PCB signal integrity analysis on one of the DGS PCBs. No simulation data present. |
| `VxWorksDocs/` | **VxWorks/Tornado 2 reference documentation archive**: `hostLib.html` (host library API), `msgQLib.html` + `msgQShow.html` + `msgQSmLib.html` (message queue APIs), `sockLib.html` (socket library); `Tornado-getStart.pdf` (Getting Started guide), `Tornado-Guide.pdf` (Tornado 2 IDE guide), `Tornado-Releasenotes.pdf` (release notes), `Vx-Progr-Guide1.pdf` (VxWorks Programmer's Guide). Useful when reading IOC/VxWorks source code — documents the APIs used in `inLoop.st`, `SendReceiveSupport.c`, etc. |

---

## `Documentation/` — Detector Database & Docs

_Explored: 2026-04-07_

| File | Contents |
|------|----------|
| `North_db.csv` | **North hemisphere detector installation database** (54 GS holes, odd numbers 1–109): columns GS#, Hoze# (crystal ID e.g. B15), Det# (physical Ge detector serial), HV (operating voltage in V), SB# (slope box number), SB pos (position on slope box: T/B/L/R/TL/BL/TR/BR), BGO# (BGO module number), BGO Install Date. 38 of 55 positions had Ge installed as of May 2017 installation. |
| `South_db.csv` | **South hemisphere detector installation database** (55 GS holes, even numbers 2–110): same column format. 43 of 55 positions had Ge installed as of May 2017. Combined: **81 Ge detectors installed** out of 110 possible positions (2017 commissioning). |
| `grey cable.xls` | Grey cable wiring reference |
| `Formal/` | System inventory XLS files (2014), software docs, unsorted docs |
| `Formal/Unsorted Docs/` | Historical PDFs/docs: `1994 bgo timing.pdf`, `1995 overview of daq.pdf`, `CDR.Oct05.doc`, `DCBAL.doc` (DC balance notes), `DG digitizer cable spec.doc`, `dg format.xls`, `DG pickoff cable spec.doc`, `dgsSender.pptx`, `dgs_systemdocs.pdf` (2011, T. Madden — earliest formal DGS system doc; describes LabWindows debugging PC: uses JNI + Java EPICS bridge to perform arbitrary VME register reads/writes via EPICS PVs, allowing LabWindows code to treat the EPICS CA layer like a NI VME bus controller; enabled firmware debugging without a native VME bus PC), `GRETINA_Fast Strobe Notes.doc`, `howTheSenderWorks.docx`. **`pv list digitizer.txt`** — very early digitizer EPICS DB (VisualDCT v2.6 format), uses `Dig$(DB)_` prefix and CAMAC-style VME addressing (`#C$(DC) S0xNN @offset`). Key PVs: `Dig$(DB)_CV_BrdId` (Board ID, addr 0x0), `Dig$(DB)_CV_FIFOStat` (FIFO status, addr 0x4), `Dig$(DB)_CS_ExtWin/NoiseWin/PileWin/TrigDly/CollTime/IntTime` (timing parameters, addrs 0x8–0x1C), `Dig$(DB)_CS_SDLLR` (SERDES lock lost reset, addr 0x500). Pre-dates current `MOD###_DIG_` naming; predates 2013 `VMExx:DIG1:` convention (see `how_to_fw2.txt`). Likely from original LBL/GRETINA-derived DGS era. |
| `Formal/Unsorted Docs/drawings/` | **DGS block diagrams and timing diagrams** (Designworks .DS4 format, mostly binary): `ANL_DIG.DS4`, `DISCCHAN.DS4` (discriminator channel), `TRGFRAME.DS4` (trigger frame timing), `TAC2.DS4`, `PEQ.DS4` (pending event queue), `ROUTER.DS4`, `PICKOFF.DS4`, `VME Block.svg`/`.jpg` (VME crate block diagram — viewable), `TRIGFP3.PDF` (trigger front panel diagram — viewable), `Waveform Extension.svg` (waveform extension diagram — viewable), `pileup readout modes.xls`. Use `VME Block.svg`/`.jpg` and `TRIGFP3.PDF` for hardware reference; .DS4 files require Designworks software to open. |
| `MeetingNotes/` | **DGS design meeting notes 2010–2012** (13 files). Key content: `dg 20100805.txt` — earliest record: resolution 2.3 keV achieved; preamp reset events spaced 46 µs apart observed; Phase I install planning (pickoff boards by Sept 2010, cables Nov 2010). `20110217.txt` — pivotal design session: decision to put clean/dirty logic in the **digitizer** (not trigger) and send 1-bit result every 20 ns to trigger; BGO Sum used (not pattern); pileup gating via CHANBIT MUX (`SEL=11` → Ge AND NOT BGO, equivalent to 'clean'). `digitizer code notes 20110515.txt` — DIG firmware rewrite plan (11 points): simplify pipeline, reduce RAM, replace SERDES machine, split discriminator timing, implement Ge/BGO triggering, cut master/slave initially, optimize for event rate, dynamic baseline tracking, skip pole-zero until tests prove necessary. `20110421.txt`, `20110714.txt` — follow-up meeting notes. `task plan 20110926.txt` — task tracking. `Digitizer firmware report 20120425.ppt` — 2012 firmware status report. Historical context for current DIG design decisions. |
| `presentations/` | Historical presentation slides |

**Note:** HV values in `North_db.csv`/`South_db.csv` are 2017 installation values — actual operating HV may differ; current values are in the EPICS snapshot PVs (`GS${N}_SlopeBoxGe_HV_RBV`). Slope box position codes: T=Top, B=Bottom, L=Left, R=Right, TL/BL/TR/BR=corner variants.

---

## `salvaged_notes/` — Key Historical Documents

Recovered notes and config files from old systems. Explored 2026-04-06.

| File | Contents |
|------|----------|
| `DGS_systemdef.txt` | VME crate to board assignment map: VME01-12, each with MDIG1/SDIG1/MDIG2/SDIG2 pairs (VME06 and VME10 have only MDIG1/SDIG1 — 2 digitizers instead of 4). Total: 10 crates × 4 DIGs + 2 crates × 2 DIGs = **44 digitizer boards × 10 ch = 440 channels** (matches ~110 GS holes × 4 signals: 1 Ge + 3 BGO) |
| `dgs_config.txt` | Python config for DB/screen generation scripts. Defines CSV register map sources (`DGSMasterTriggerRegisterMap.csv`, `DGSRouterTriggerRegisterMap.csv`, `MasterDigitizerRegisterMap.csv`), VME crate/slot assignments (MTRG in crate 32 slot 3; VME01-12 with MDIG/SDIG in slots 3-6), and EPICS runtime path (`/global/devel`). Historical precursor to current IOC config. |
| `FW_MAINT_README.txt` | Firmware flashing note: refers to old Java-based EPICS client (`flashdgs.js`, `runJEpics`) on con6 -- historical, superseded by current Python/EPICS approach |
| `how_to_python.txt` | **2013 IOC build session log** (terminal recording from `dgs1`, 2013-08-30, user `tmadden`). Documents the first successful `gretDet.munch` build using the Solaris+Tornado2.2 toolchain on `con5`. Key facts: (1) First build attempt failed due to duplicate member declarations in `asynDigParams.h` — fixed between attempts. (2) Build environment: Tornado 2.2 on con5 (`/home/root2/dgs/devel6/gretTop/9-22/`), Solaris cross-compiler to `vxWorks-ppc604_long`. (3) IOC was `dgsIoc/tcDetApp`, linking `libdevDGSDriverSupport.a` + `libdevGVMESupport.a`. (4) DB templates installed: `dgsDigRegisters.template`, `dgsDigUser.template`, `dgsGlobals_DGS_VME01-VME11.db`, `dgsGlobals_DGS_GLBL.db` — **only 11 VME crates** (VME01–VME11), not 12. (5) Also shows CSS/OPI screen SVN update (rev 3045) pulling `CLOCommanderR.opi`, `AllCommander.opi`, `diglink_setupcloR`, etc. — shows transition from DGS to CLO/DFMA naming in CSS screens circa 2013. Historical predecessor to `vxworks/` Git repo. |
| `how_to_fw.txt` / `how_to_fw2.txt` | **2013 firmware flashing session logs** (terminal recordings from `dgs1` Solaris host). Shows `epics.sendFpga(digware, fn1, crate, slot, ...)` JavaScript API sending 1.66 MB bitfiles via EPICS to `VMExx:DBG:fpga_erase/program/verify_RBV` PVs. Also shows `checkfpgas.py` output (2013-08-30) confirming all VME01–VME11 DIG boards at rev 0xB.0 (date 0x20130808), except VME08:DIG3 = 0xFFFFFFFF (bad/unresponsive board). **Key historical fact:** PV naming in 2013 was `VME01:DIG1:code_date_RBV` / `mjr_code_revision_RBV` / `min_code_revision_RBV` — the `DIG1`/`DIG3` prefix vs current `MDIG1`/`SDIG1` prefix. Also: VME32 MTRG/RTRG PVs (CODE_DATE, CODE_REVISION) were not yet connected in 2013. |
| `NotesOnInloop.txt` | Detailed analysis (JTA) of `inLoop.st` state machine PV hooks and queue usage: `SCAN_FOR_DATA` calls `transferDigFifoData()` and `DigitizerTypeFHeader()` with type codes (0=empty, 1=end-of-run, 2=error). Confirms `TypeOfBoard` (board_type) field is populated but not used by receiver. |
| `20180924_notes.txt` | Architecture notes on FIFO readout hierarchy: `inLoop.st` -> `checkDigFIFO()` (in `readDigFIFO.c`) -> `serviceOneBufferDig()`. Documents `rawEvt` struct fields (`board`, `len`, `refcount`, `curptr`, `bptr`, `owner`); queue sizes: `RAW_Q_SIZE=400`, `RAW_BUF_SIZE=512KB`. |
| `jta_notes_20200501.txt` | JTA notes on VxWorks build environment: `munch` files, EPICS base paths (`/global/devel/base`, `/global/devel/supTop`), EPICS base versions 3.14.10 and 3.14.12.1. Historical context for vxWorks migration. |
| `ChangeNotes_20220801.txt` | **Key design decision (Aug 2022):** Eliminated requirement that trigger data must look like digitizer data. Updated `SendReceiveSupport.c` and receiver (`dgsReceiver`). Non-digitizer data dumped to `<run_name>_DIAG` file. Type `0xFF` = unspecified (backwards compat); type `0x00` = normal digitizer. Trigger data has GEB type 15=DGSTRIG. Authors: M. Oberling + J. Anderson. |
| `WikiExtract.txt` | Firmware flashing procedure (historical): log into dgs1, get `*.bin` from `/Digitizer/MAIN_FPGA/Work11_DGS` (4 flavors: MSTR_digitizer, SLAVE_digitizer, trigger_top, router_top), copy to `/home/dgs/tmadden/DGSDigFirmware`, then run `java -classpath jca-2.3.5.jar:caj-1.1.9.jar:fpgasender.jar plotControl`. Historical Java-based flashing tool (`fpgasender.jar`), superseded by current Python/EPICS approach. |
| `DecodingTheMakefile.txt` | Notes on EPICS makefile traversal in the con6 build environment |

---

## `findFile.sh`

A utility to locate files within the large SVN tree by name pattern.

```sh
./findFile.sh <pattern>
```

---

## Usage / Status

This is primarily an **archive** and **reference** — not actively developed. The active development has migrated to Git repositories (fpga/, ioc/, vxworks/, etc.).

Useful for:
- Finding historical implementations
- Recovering old configuration files
- Understanding the DGS1 → DGS2 migration
- Looking up Solaris/con6-era build environments

---

## Cross-References

| Topic | File |
|-------|------|
| VxWorks build pipeline; migration from con6/Solaris to Ubuntu | [`vxworks_migration.md`](vxworks_migration.md) |
| FPGA firmware overview (current active firmware, derived from SVN era) | [`fpga.md`](fpga.md) |
| EPICS IOC (current Git version; pre-Git history in `DGS_SVN/dgsSoftIOC/`) | [`ioc.md`](ioc.md) |
| SBX/SlopeBox: GS_ID dongle, BGO HV map, pickoff card | [`sbx.md`](sbx.md) |
| ANLDAQ/tcpReceiverMT — supersedes legacy `gtReceiver/` in SVN | [`ANLDAQ.md`](ANLDAQ.md) |
| MγRIAD hardware documentation (PDF manual, memory map) | [`myriad.md`](myriad.md) |
| PSG auto-DB generation tool (used CSV register maps; see `psg/`) | Not yet documented — see `DGS_SVN/psg/` in local SVN checkout |
| PDF index — includes many docs referenced from this SVN | [`PDF_index.md`](PDF_index.md) |
| Hardware drawings / reference index | [`reference_index.md`](reference_index.md) |
| Data structures: GEB header formats (GEB_TYPE_DGS=14, DGSTRIG=15) | [`data_structures.md`](data_structures.md) |
| Digitizer Tester hardware (full documented version) | [`digitizer_tester.md`](digitizer_tester.md) |
| N=126 target wheel encoder interface (`psg/N126_target_wheel/`) | [`n126_target_wheel.md`](n126_target_wheel.md) |
| Collector box: `NewBlackBox/` pre-EPICS Pi code history | [`collectorbox_devicesupport.md`](collectorbox_devicesupport.md) |

---

*Source: `/home/dgsspark/DGS_tools_pack/DGS_SVN/dgs/`. Explored: 2026-04-06–2026-04-15. Status: archive — exhausted, all key areas documented.*
